logging:
  level:
    org.springframework: DEBUG

# 다국어 리소스 파일 명 지정
spring:
  messages:
    basename: messages

# Actuator에서 더 많은 정보를 보기 위한 설정
management:
  endpoints:
    web:
      exposure:
        include: "*"
